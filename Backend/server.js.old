const port = 3308;  
var mysql = require('mysql');  
var express = require('express');
const bodyParser = require('body-parser');
var app = express(); 

var connection = mysql.createConnection({
	host     : 'eecslab-9.case.edu',
	user     : 'team_7',
	password : 'e6444a4a',
	database : 'team_7'
});
var hostport = process.env.PORT || 3000;


var userquery =[{
	type:'input',
	name:'name',
	message: "Which user do you want to query?"
}]

connection.connect() 
console.log('Connected to the MySQL server.');
app.listen(hostport);

connection.query('SELECT * FROM user WHERE U_NAME = ?', [answers['name']], function (err, rows, fields) {
  if (err) throw err

  console.log('The user\'s ID is: ', rows[0].U_ID)
})


